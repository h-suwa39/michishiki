# 改訂履歴

## 0.6.0 — 2026-09-06

- 設定のメニュー名「マイカスタマイズ」を「マイカスタム」に
- 「いつもの」を設定の 4 つ目のメニューに。画面の「いつもの」ボタンからも、右上の歯車からも同じ場所に着く
- 切りかわりの動きを 3 つから選べる。「手ばなす」（1 回きりのできた分を消す。くりかえしは翌朝「まだ」に戻して回数 0 で残す。セットを置く）／「手ばなさない」（やることはそのまま、セットは置く）／「なにもしない」（日付が進むだけ）。データ欄の「できた分を手ばなす」でいつでも手動で手ばなせる
- 「1日の切りかわり」— マイカスタムで、きょうが新しくなる時刻を 0〜7 時から選べる。既定は朝 4 時（夜ふかし中に日付が変わらない）。切りかわり前の深夜は日付の横に「まだきょう」と出る。いつものセットの曜日もこの「アプリの一日」で判定。開いたままでも 1 分ごとに切りかわりを確認
- 「くりかえし」— おむつ替えやミルクのように 1 日に何回もあることを、1 行のまま回数で数える。丸をタップするごとに 1 回増えて丸の中に回数が出る。小さな演出つき。行は残る
- 辞書のミルク・離乳食・おむつ替え・こどもの薬・水を飲む・薬をのむ・ペットのごはん・ペットの散歩は最初から「くりかえし」で置かれる。編集シートでどの言葉でも切り替え可
- 編集シートに「1回もどす」「きょうはここまで」。後者で「できた」に沈む
- 進みぐあいの輪は 1 回きりのやることで数え、くりかえしは「くりかえし 3 回。」のように回数で表示。全部できたときの祝福は、くりかえしが残っていても出る
- 日付が変わると、くりかえしは回数 0 で持ち越し。いつものセットにも「くりかえし」が保存される

## 0.5.1 — 2026-09-06

- iPhone で右端が切れていた不具合を修正。「ならべる／わける」と右側のボタンの行が画面幅を超えるとページ全体が広く組まれていたので、狭いときは右側を下に折り返すように。トーストも折り返し可能に
- 完了の演出の粒が画面に残ることがあったので、描画面を使わないときは非表示にし、消去も確実に
- 入力欄の候補を自前の表示に。入力欄をタップすると、いまの時間帯（あさ・ひる・よる）に合う言葉と自分の言葉が上に横並びで出て、タップで即置ける。文字を打つと絞り込み。「もっと…」で辞書へ。ブラウザ標準の候補リスト（datalist）は廃止
- 「よく使うことば」を開くとき、いまの時間帯を最初から選んだ状態にする
- iOS の Chrome などでブラウザのツールバーやキーボードに隠れていた入力欄・シート・トーストを、実際に見えている範囲（Visual Viewport）に合わせて持ち上げる
- 入力欄の列を一覧の列とぴったり揃え、横方向のはみ出しを止めた
- 歯車ボタンから設定を開くとメニューの中身が出ないことがあった不具合を修正

## 0.5.0 — 2026-09-05

- 確認はブラウザ標準のダイアログではなく、アプリの見た目に合わせた確認カードに。消す系は赤いボタンで、最初のフォーカスは「やめる」側
- 辞書メニューに「使うことば」— 辞書の言葉を一つずつ ON/OFF。グループごとに「すべて使う／使わない」
- 最初は OFF の言葉を追加（こどもの通院、こどもの薬、幼稚園・学童の送迎、宿題を見る、おもちゃの片づけ、離乳食のストック作り、保護者会・PTA、家族のデイサービスの送り出し、介護の手続き、在宅の仕事の時間、資格の勉強、通院）
- スマホでも「わける」のカードをドラッグできるように。長押しでつまみ上げ、指の下の象限が光り、離すと置かれる
- 「ならべる」の行も同じ仕組みで並べかえ。長押し（PC はそのままドラッグ）でつまんで、行の間に落とす。矢印も引き続き使える

## 0.4.0 — 2026-09-05

- 全部できたときの見出しは、句ごとに折り返し位置を指定。390px 幅で 15 パターンすべて、語の途中や句点だけの行が出ないことを確認
- 「いつものセット」— 平日用・休日用のように、いつものやることを名前と曜日つきで保存。きょうの一覧からワンタップで保存でき、曜日を決めておくとその日の朝に自動で置かれる（すでにある言葉は重ねない）。手で「きょうに置く」も可能。中身は「いまの一覧で更新」で置きかえ

## 0.3.0 — 2026-09-05

- 「くらしのかたち」— 設定で、こども・パートナー・同居の家族・ペットの有無と名前を登録できる。辞書の中身がそれに合わせて変わる
- 辞書のテンプレート — 「{こども}の検温」のような言葉は、名前があれば「はなの検温」「そうの検温」と人数分に増え、無ければ「検温」になる。パートナー・家族・ペットの言葉は「いる」にしたときだけ出る
- 「かぞく・ペット」グループを追加（該当するときだけ表示）
- 辞書にことばを足す — 自分の言葉をタグ付きで辞書に登録、× で消せる
- 全部できたときの見出しを 10 パターン以上に。夜は「おやすみ」系も混ざる
- 設定を「カラーテーマ」「マイカスタム」「辞書」の 3 メニューに。マイカスタムではパートナーの呼び方（夫・妻・彼氏・彼女・パートナー・相方…や自由入力）と同居の家族（母・義母・祖父…）をワンタップで選べる。辞書メニューで足したことばを管理
- 「すべて消す」を「きょうのやることを消す」に。くらしのかたちと辞書は残す
- Service Worker で外部の応答をキャッシュする際のエラー（Response body is already used）を修正

## 0.2.0 — 2026-09-05

- 「よく使うことば」— 入力欄の左の本のボタンから、タップで置ける辞書。「いつ」（あさ・ひる・よる・ときどき）と「なに」（こどもと・ごはん・おうちのこと・じぶんのこと・おでかけ・手続き・れんらく）の 10 グループで、ひとつの言葉が複数のグループに属する。掛け合わせで絞れる。自分で入れた言葉は「じぶんのことば」にたまる
- 入力欄に候補を表示（自分の言葉と辞書の言葉）
- Cloudflare Workers の静的アセット配信で公開できるよう `wrangler.jsonc` と `.assetsignore` を追加
- 全部できたときの言葉を「きょうも、がんばってくれてありがとう。」に。ボタンは「こちらこそ」
- サブタイトルや祝福表示の折り返しを、語の途中で切れないように直した

## 0.1.0 — 2026-09-05

最初の公開版。

- 「ならべる」— 一列表示。上下の矢印で順番を入れかえる
- 「わける」— 4象限表示。まだ分けていないものはトレイに置き、タップまたはドラッグで象限へ
- 「4象限の順にならべる」— 象限の位置から一列の順番を組みなおす
- 完了時のひとことと小さな粒の演出。全部できたときの祝福表示。設定で演出を切れる
- 日付が変わると、できた分を手放し、まだの分だけを持ち越す。持ち越し件数をお知らせ
- 色のテーマ 5 種（あい・あさやけ・わかば・ゆうなぎ・こはく）と、外観（端末にあわせる／明るい／暗い）
- データはこの端末の `localStorage` にだけ保存。JSON ファイルへの書き出し・読み込み、すべて消す
- PWA（マニフェスト・Service Worker・道標のアイコン）
